[tool.robotpy]

# Version of robotpy this project depends on
robotpy_version = "2026.2.2"

# Which packages to install on the roboRIO
requires = [
    "robotpy-commands-v2",
    "phoenix6",
    "robotpy-pathplannerlib",
]
